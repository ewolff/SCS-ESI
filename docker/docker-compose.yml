esi-catalog:
  build: ../scs-demo-esi/scs-demo-catalog-esi
esi-order:
  build: ../scs-demo-esi/scs-demo-order-esi
esi-varnish:
  build: varnish
  links:
   - esi-order
   - esi-catalog
  ports:
   - "8080:8080"

